package command.page;

import javax.servlet.http.HttpServletRequest;

public class FinishedVisitsPageCommand implements command.Command {
    @Override
    public String execute(HttpServletRequest request) {
        return null;
    }
}
